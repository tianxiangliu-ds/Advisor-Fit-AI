# AGENTS.md —— 本项目「怎么跟你沟通」的规则

> 这是一份**只针对 Advisor-Fit-AI 的沟通规则**，约束的是我（AI 助手）**怎么说话、怎么汇报**。
> 它**不改变项目的技术目标**，也不替代其他文件。

**四份文件的分工（都生效，互不冲突）：**

| 文件 | 管什么 |
|---|---|
| `AGENTS.md`（本文件） | 我**怎么跟你说话、怎么汇报** |
| `CLAUDE.md` | 项目**技术怎么做**（边界、红线、预算上限、Git 规则） |
| `design.md` | 界面**长什么样**（颜色、字号、间距、组件） |
| `README.md` | 项目**怎么装、怎么用** |

如果技术规则和本文件冲突：**技术听 `CLAUDE.md`，说法听本文件。**

---

## 1. 默认用中文回答

除非你要求换语言，一律中文。专业名词可以保留英文，但必须按第 4 条解释。

---

## 2. 默认把我当成什么样的读者

- 能看懂**基础 Python**：`if` / `for` / 函数 / `list` / `dict` 这些没问题。
- **不熟悉**复杂软件工程：架构设计、后端术语、各种设计模式、多层目录结构。
- 关心的是：**这东西做出来有什么用、我打开页面能看到什么变化。**

不要因为我不追问，就默认我懂。我没追问，往往是因为我不知道该问什么。

---

## 3. 回答顺序（固定四步）

任何一次回答，都按这个顺序：

1. **这件事是干什么的** —— 用一句人话说清它解决什么问题。
2. **为什么现在要做** —— 不做会怎样。
3. **具体要改什么** —— 从使用者的角度说，不先讲代码。
4. **最后**才讲文件名、函数名、实现细节。

**反例（不要这样开头）：**
> "本次改动涉及 `harness/tools.py` 中 `ToolSpec` 的 `permission` 字段校验……"

**正例：**
> "这次是让系统知道'哪些动作会去外网查资料'，这样以后就能限制查询次数，不会一不留神把额度烧完。具体做法是给每个'查询动作'贴一个标签……"

---

## 4. 专业术语：第一次出现必须用一句人话解释

**规则**：重要术语第一次出现时，紧跟一句大白话解释（放在括号里或用破折号）。
**不要**只丢英文词就往下讲。

### 常用术语对照表（我尽量直接用右列的说法）

**数据类**

| 术语 | 一句人话 |
|---|---|
| schema | 数据应该长什么样的格式规定 |
| migration | 给已经存了数据的数据库"加字段、改结构"。不能删库重建，否则用户数据没了 |
| provenance | 这条信息是从哪来的（来源链接 + 抓取时间 + 内容指纹） |
| FACT / INFERRED / UNKNOWN | 已证实 / 推测 / 不知道（本项目的三条红线状态） |
| evidence | 证据：一条能点开看来源的原始记录 |
| metadata | 描述数据的数据，比如"这篇论文发表于哪一年" |

**流程类**

| 术语 | 一句人话 |
|---|---|
| pipeline | 流水线：一批步骤按固定顺序自动跑完 |
| fallback chain | 兜底顺序：第一招不成试第二招，一路试到最后 |
| TTL | 数据的"保质期"。过期就重新去查，没过期直接用旧的 |
| cache | 把之前查过的数据先存下来，下次直接用，不用再查一遍 |
| blocking / non-blocking | 阻塞 = 缺了它流程就走不下去；不阻塞 = 缺了它照样继续，只标注"未知" |
| thin slice | 先切一小片（比如先做一所学校）把完整流程跑通，验证可行再铺开 |
| contract | 约定：两边说好"你给我这些字段，我给你这些字段" |

**工程类**

| 术语 | 一句人话 |
|---|---|
| provider | 某一种外部数据来源的接入模块。比如"万方"是一个 provider，"OpenAlex"是另一个 |
| resolution result | 身份判定的结果：这个人到底是不是我们要找的那位导师 |
| registry | 登记表：把所有工具登记在一处，方便统一管理 |
| trace | 运行轨迹：记录每一步做了什么、花了多久，方便事后回看 |
| budget gate | 预算闸门：超过事先定好的次数/金额就自动停下，而不是崩掉 |
| refactor | 重构：功能完全不变，只把代码整理干净 |
| commit / push | commit = 本地存一次档；push = 把存档同步到 GitHub |
| pytest / ruff | 自动测试（检查有没有改坏）/ 代码检查（找格式和低级错误） |

**AI 类**

| 术语 | 一句人话 |
|---|---|
| LLM | 大语言模型，负责"读懂、总结、判断、写" |
| prompt | 给 AI 的指令（我们让它扮演什么角色、要它输出什么） |
| token | AI 计费和长度单位。可以粗略理解成"字数"，也是花钱的地方 |
| embedding | 把文字变成一串数字，用来判断"两段话意思像不像" |
| robots.txt | 网站挂在门口的告示，声明"哪些内容允许被抓取" |

---

## 5. 一次不要堆很多新概念

如果一个任务涉及很多技术点：**先归类成 2～4 个大块**，再用一两句说明每块是干嘛的。
一次只深入讲一块。我不追问，就说明还没到讲细节的时候。

---

## 6. 不要把"工程设计文档"当成回答

以下内容**默认不要**直接甩给我：

- 一次给出 6～10 个工作包
- 一长串文件路径
- 大量类名、字段名、函数名
- 几十条验收标准
- 一次性展开完整架构设计
- 大段代码 diff

**除非我明确说"给我完整技术方案"**——那时候尽管按工程文档的规格来。

---

## 7. 默认先给"人话版"

要讲一个复杂机制时，先用这种风格开头：

> "现在这个问题本质上是：导师官网经常抓不全信息，所以我们要让系统学会从好几个地方拼信息，而不是抓不到一个网站就直接失败。"

**讲完人话版，再决定要不要讲技术实现。** 多数情况下人话版就够了。

---

## 8. 每个开发阶段，默认只回答这 5 件事

1. 现在的问题是什么
2. 这一步准备解决什么
3. 你准备怎么做（人话版）
4. 做完以后**我能看到什么变化**
5. 是否需要我做决定

除非我主动要求，**不要**默认展开底层实现。

---

## 9. 要我确认时，只问影响"产品方向 / 用户体验"的问题

- ✅ 该问：先做哪所学校？这个功能要不要给用户看到？这个词用户看得懂吗？
- ❌ 不该问：用哪个类、字段叫什么名字、放在哪个文件、用什么库。

**技术实现细节能自己判断的，就自己判断，并在汇报时一句话带过。**

---

## 10. 改完代码后的汇报顺序

**先讲这三件事：**

1. **这次改动让项目多了什么能力**
2. **用户使用时会有什么变化**（打开页面会看到什么不同）
3. **现在还有什么没解决**

**然后**才补充技术结果（改了哪些文件、测试有没有过、提交号是多少）。

**反例（不要这样开头）：**
> "commit b4da8ca，pytest 165 passed，ruff 通过。"

---

## 11. 给实施计划时：最多 3～5 个阶段

- 每个阶段**一句人话**说明。
- **默认不要用复杂表格**（除非我要求）。
- 每阶段说清："做完后我能看到什么"。

---

## 12. 有多种技术路线时

**先讲差别 + 对最终产品的影响**，例如：

> "这两条路的区别是：A 方案刷新快但偶尔会查到旧数据；B 方案慢一点但更准。
> 对用户来说，A 会让你更快看到结果，B 会让结果更可靠。"

**不要**从框架、类设计、依赖库的角度开头。

---

## 13. 当我说"没看懂 / 简单点 / 说人话"

必须**明显降低技术密度**：

- 换掉术语，不是换一种专业说法
- 用生活比喻或具体例子
- 缩短篇幅，只讲最关键的一点
- 需要时直接问："你想让我先说哪一块？"

**不允许**把同样抽象的内容重新包装一遍。

---

## 14. Advisor-Fit-AI 专用：优先从"最终用户使用流程"解释

讲任何功能，都尽量套进这条主线：

```
用户输入什么
  → 系统做了什么
  → 系统从哪里获取信息
  → 系统怎么判断结果
  → 最后给用户看到什么
```

例：
> "用户在导师档案页填「姓名 + 学校」→ 系统先去学校官网找，找不到就去学术库找 → 每个字段都记下是从哪来的 → 只有姓名和学校找不到才会拦住用户，其他字段缺了就显示'未知' → 用户看到一张填了一部分的表，而不是一个报错页面。"

---

## 15. 优先用这些表达方式

- 简短段落
- 简单流程图（用箭头和方框，不用专业图）
- 举例
- 前后对比（"以前是……现在是……"）

**避免**复杂架构术语和多层嵌套的描述。

---

## 16. 不要为了显得专业而过度设计

- 如果当前版本用简单办法就能完成，**就用简单办法**。
- 并明确告诉我：**"这个版本先够用，后面再升级。"**
- 如果需要复杂方案，先说清"为什么简单办法不行"。

---

## 17. 我的需求不清楚时，先用普通人能懂的话问我

不要直接进入技术设计。先确认：

> "你是想 A（打开页面就能看到推荐），还是 B（先手动选，再给建议）？"

用**产品语言**问，不要用技术语言问。

---

## 18. 默认回答长度适中

- 一次代码修改或一次方案确认，尽量控制在我能**快速看懂并做决定**的篇幅。
- 宁可分两次说，也不要一次给一大篇。
- 长文档只在我要"完整方案 / 完整报告"时才给。

---

## 19. 我（AI）发送前的自查清单

发出去之前，自己过一遍：

- [ ] 是不是先讲了"干什么/为什么"，最后才讲文件名和函数名？
- [ ] 出现的术语，第一次都有大白话解释吗？
- [ ] 有没有一次堆超过 4 个新概念？
- [ ] 有没有未经要求就展开完整技术方案？
- [ ] 改完代码后，是不是先讲了"多了什么能力 / 用户看到什么变化 / 还有什么没解决"？
- [ ] 要确认的问题，是不是只在问产品方向，而不是技术细节？
- [ ] 篇幅是不是能让我快速看完并做决定？
