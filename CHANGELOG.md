# 更新日志

本项目遵循「一个可独立验证的功能一次提交」的节奏；版本号同时写在
`pyproject.toml` 与 `src/advisor_fit/__init__.py`，界面上显示的是后者。

## v0.2.0（进行中 · 目标：用户可用的 1.0）

**论文检索多源化——不再依赖万方 Key**

- 新增检索路由器：按学科分流查多个免费学术库，跨库合并去重
- 新增数据源：Europe PMC（生物医学）、arXiv（预印本）；OpenAlex 改为
  「先查作者档案、机构对得上才认」的高精度路径；Crossref 只用于按标题补全
- 新增严格姓名核对：挡掉学术库分词模糊匹配带回来的同姓别人
- 新增中英机构别名比对：中国导师的英文论文不再被误判成「机构不符」
- 检索源登记表可用 `config/sources.json` 覆盖：加库 / 调优先级 / 开关都不用改代码
- 万方与 AMiner 降为可选增强源，缺 Key 时跳过并说明原因

**产品形态：从「研究某一位」扩展到「给一个方向 → 推荐几位候选」**

- 新增「◌ 方向找导师」页：研究方向关键词 → 本地导师库粗筛 → 可解释排序 → 并排比较
- 「从导师库填充」改为优先查统一导师库（3 万条）

**开源发布准备**

- **采用 MIT 许可证**（见 `LICENSE`），README 增加许可证与免责说明
- 面向使用者的 README（截图 + 一句话价值 + 快速开始）与可复现的界面截图
- README 补上「Agent 的每一步都看得见」：把运行轨迹截进界面一览
- 数据备份（命令行 + 页面下载）、上传简历清理规则、版本号显示与一致性检查、GitHub CI
- **在线 Demo 一键部署**：`streamlit_app.py` 演示入口（自动生成全部虚构的演示数据）
  + `requirements.txt`（从 pyproject 同步）+ `Dockerfile` + Streamlit Community Cloud 说明

**已知限制**（详见 README 的「已知限制」）

- DBLP 已启用 Anubis 反爬校验，默认关闭，计算机领域召回由 OpenAlex 承担
- 常见中文名 + 没填英文名时精度有限，系统只认「机构对得上」的作者档案
- OpenAlex 的作者档案本身可能有合并错误，界面会显示本次用的是哪个档案

## v0.1

- 上传 PDF 简历、本机解析、学生事实人工确认
- 导师资料手动录入 / 主页解析 / 字段级补齐 / 身份核对 / 网页缓存 TTL
- 万方检索 Agent + 作者消歧 + 人工确认门控（可选在线能力）
- 证据驱动的导师画像、分维度匹配、邮件草稿、Markdown/JSON/Word 导出、完整删除
- 离线可跑：不联网、不配任何 Key 也能完成全流程
