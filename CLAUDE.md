# 项目规则

## 最终愿景（北极星）

Advisor-Fit AI 的最终目标是面向国内硕博双选/套磁场景的「导师研究匹配 Agent」，而非停留在手动证据录入工具。三层架构：

- **LLM** = 推理核心：负责理解、抽取、总结、判断、生成。
- **Agent** = 任务执行：自主决定下一步查什么、调用什么工具、何时重试或请求人工确认。
- **Harness** = 执行控制层：管理 Prompt、模型、Tools、状态、权限、证据、失败重试，以及多轮 LLM→Tool Call→Tool Result→LLM 循环。

目标流程：上传简历 → LLM 将教育/技能/项目/科研/兴趣结构化为可核验学生画像 → 输入导师姓名+学校+主页 → Agent 调用网页搜索、学术数据库、论文元数据 API、PDF 解析等工具，自主完成「导师身份确认 → 近年论文搜集 → 作者消歧 → 摘要与关键词抽取 → 研究方向归纳 → 证据整理」→ LLM 基于学生画像与导师证据做语义匹配与推理（研究交集、方法能力匹配、背景缺口、最值得读的论文、联系前应补的知识）→ 生成每个关键陈述都能追溯到 CV Fact 或论文 Evidence 的个性化联系邮件。

证据三态（FACT/INFERRED/UNKNOWN）、防幻觉、可追溯仍是产品红线：Agent 的自动产出必须可追溯到证据，并经人工确认门控。

## 项目定位

这是一个面向中国高校硕博导师双选的、本地优先、证据驱动的单导师匹配助手。

v0.1 的核心不是自动找导师，而是：用户上传 CV，人工确认学生事实；用户手动录入已核实的导师信息、论文标题、摘要和来源链接；系统生成带证据的导师画像、匹配分析和中文邮件草稿。

## 当前版本边界

> 以下边界是 v0.1 当前实现阶段的约束；最终产品走向上面的「导师研究匹配 Agent」，届时联网检索与 LLM 将从「可选」变为「核心能力」。v0.1 先守住离线、可核验、防幻觉的底线，作为 Agent 的安全底座。

1. 默认不抓取导师官网，不调用 OpenAlex，不爬取知网、Google Scholar 或其他学术网站。
2. 不自动搜索导师，不做作者消歧，不做批量导师比较，不发送邮件。
3. 必须支持离线、无 LLM API 的完整流程：CV 解析 → 人工确认 → 手动导师资料 → 手动论文证据 → 报告 → 模板邮件 → 导出。
4. LLM 从 v0.2 起成为结构化抽取/推理核心，但仍必须可降级：无 LLM 时用规则/模板兜底，核心流程不因 LLM 缺失而不可用。LLM 供应商多接口可切（DeepSeek 首发，OpenAI/Anthropic/Ollama 可插）。
5. 用户输入的导师资料和论文必须标记为“用户已确认”，系统不得将其伪装成自动抓取或官网确认。

## 事实与证据规则

1. 严格区分 FACT、INFERRED、UNKNOWN。
2. 所有导师事实必须关联至少一条 Evidence，包括论文标题、摘要、来源 URL。
3. 所有用于邮件的学生经历必须来自用户明确勾选确认的 CV 事实。
4. 没有证据时必须输出 UNKNOWN 或 INSUFFICIENT_EVIDENCE，绝不补全、猜测或编造。
5. 不得把“研究匹配”解释为“导师正在招生”或“录取概率高”。
6. 不得声称用户已阅读论文，除非用户明确确认。
7. 不得生成“拜读、久仰、震撼”等未经证实的恭维表达。

## 产品体验规则

1. 面向非技术用户，页面文案必须使用中文和清晰的行动提示。
2. CV 自动解析结果只是候选项；用户必须能编辑、删除、增加和确认。
3. 导师姓名、学校/单位必须必填。
4. 每篇论文至少要求标题、摘要、http/https 来源链接和“论文归属确认”。
5. 无关键词时可用论文标题与摘要做保守匹配；有关键词时优先使用关键词。
6. 无 LLM API 时必须生成事实锁定的确定性邮件模板，不能留空。
7. 删除本次数据必须同时删除本次数据库记录和上传的 PDF。

## 工程规则

1. 先读 README、现有模型、测试和 Git 状态，再开始改动。
2. 每个功能或修复必须先写失败测试，再写最小实现，再运行测试。
3. 不重写无关模块；优先复用现有 Evidence、StudentProfile、ProfessorProfile、MatchReport、Draft 等模型。
4. 所有网络能力必须是可选插件式能力，不能阻断手动输入流程。
5. Agent 底座采用自研轻量 Harness（照 Pi 的 pi-ai 多模型层 + pi-agent-core 循环层思想，Python 实现），不引入 LangChain / LangGraph / CrewAI 等重框架；工具层能力（PDF 解析、网页抽取、OCR、embedding、学术库客户端）优先复用现成开源库，不重复造轮子。
6. 每次修改后运行相关测试；完成前必须运行全量 pytest、ruff 和评测脚本。
7. 发现旧测试失败时，先定位根因并说明，不要为了让测试通过而删掉测试或降低验证标准。

## Git 规则

1. 当前项目的 v0 基线由 Git 标签 v0 保存；v0.1 由 Git 标签 v0.1 保存。
2. 不复制整个项目文件夹保存版本；使用 Git 提交、标签和 diff 管理版本历史。
3. 每个可独立验证的功能使用清晰提交信息，例如：
   - `feat: add manual paper evidence input`
   - `fix: remove uploaded CV when deleting run`
   - `test: cover offline manual pipeline`
4. 未经用户明确允许，不执行 git reset --hard、强制推送、删除分支或推送到 GitHub。
5. 提交前先展示 git diff 摘要，并确保工作区只包含与当前任务相关的修改。

## 完成定义

只有在以下条件全部满足时，才能说任务完成：

- 用户可用一份真实或匿名 CV 成功跑通完整流程；
- 不联网、不配置 OpenAlex、不配置 LLM API 时仍能生成报告和邮件模板；
- 每个导师结论均可回溯到用户输入的导师资料或论文证据；
- 邮件没有虚构学生经历或导师研究；
- 测试、代码检查和评测全部通过；
- README 已同步更新使用方式、边界和已知限制。

## 工作方式

先用简短中文说明你准备修改什么、为什么改、会影响哪些文件。

- 如果需求会扩大产品边界，先给出方案和取舍，等待确认后再实现。
- 如果可以在现有边界内安全推进，则直接实现，不要反复询问已经明确的信息。
